[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run python -m plaindr serve --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 30
